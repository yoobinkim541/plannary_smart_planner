{
  "name": "planary-eclass-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted background worker that syncs SeoulTech e-class every 5 minutes",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "once": "node index.js --once"
  },
  "comment": "Worker reuses the root node_modules (see ../package.json). Do NOT add dependencies here — installing them creates a second firebase-admin instance and breaks credential propagation through api/eclass/_admin.js.",
  "dependencies": {},
  "engines": {
    "node": ">=20"
  }
}
